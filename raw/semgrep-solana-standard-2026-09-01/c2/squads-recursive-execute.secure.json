{"version":"1.174.0","results":[{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":211,"col":20,"offset":8626},"end":{"line":212,"col":10,"offset":8641},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":324,"col":36,"offset":13860},"end":{"line":324,"col":42,"offset":13866},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-010-init-if-needed","path":"/src/src/lib.rs","start":{"line":346,"col":61,"offset":14765},"end":{"line":346,"col":80,"offset":14784},"extra":{"message":"Jelleo SOL-010: reinitialization / account takeover — matched on the PROPERTY, not one idiom. Fires on Anchor `init_if_needed` AND on a hand-rolled reinit: a raw account-header write via `.data.borrow_mut()` / `try_borrow_mut_data()`. If a permissionless handler (re)writes an account WITHOUT first proving it is FRESH (8-byte discriminator all-zero, `is_initialized == false`, or `#[account(zero)]`), a second call re-inits a LIVE account: the caller-chosen authority/owner overwrites the stored one, and a downstream `has_one = authority` instruction (withdraw/close) then trusts the attacker -> takeover -> drain. `owner == program_id` proves ownership, NOT freshness. SAFE form: read `data[0..8] != [0;8]` and reject an already-initialized account (Orca `initialize_dynamic_tick_array`), or plain `init` / `#[account(zero)]` + an existence guard checked before any payout. Real incident: Metaplex Candy Machine v1 config-drain. See: github.com/Copenhagen0x/solana-security-standard#sol-010.","metadata":{"sol-id":"SOL-010","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-010"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the account holds no lamports, token balances, authority keys, OR access-gating state that a second initialization could reset — access-gating state includes (non-exhaustively) a timestamp / nonce / status used as a lock, AND any field that controls subsequent PDA derivation, account creation, or role assignment","an explicit already-initialized guard REJECTS a second initialization before any write — for a hand-rolled `.data.borrow_mut()`/`try_borrow_mut_data` write the 8-byte discriminator is asserted all-zero (`data[0..8] == [0;8]`) or an `is_initialized`/version flag asserted false and the handler returns Err when already set (Orca `initialize_dynamic_tick_array`), for Anchor it is plain `init` / `#[account(zero)]`; a bare `.data.borrow_mut()` that only writes a data region of an already-type-validated account (discriminator/authority untouched) also clears this","the value-moving path is gated by a nullifier/spent-marker created with plain `init` so a duplicate redemption ABORTS AT ACCOUNT CREATION before any payout - NOT a mutable boolean flag in an existing account (a flag a reset/reinit path can re-open does NOT clear this rule)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-009-invoke-signed","path":"/src/src/lib.rs","start":{"line":422,"col":21,"offset":18992},"end":{"line":422,"col":35,"offset":19006},"extra":{"message":"Jelleo SOL-009: `invoke_signed` CPI. Verify the CALLER has authority for this operation BEFORE the CPI — `invoke_signed` just signs with a PDA, it doesn't validate upstream authority. See: github.com/Copenhagen0x/solana-security-standard#sol-009.","metadata":{"sol-id":"SOL-009","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-009"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the caller's authority for THIS SPECIFIC privileged operation is verified (Signer + has_one/constraint binding the caller to the target resource) BEFORE the invoke_signed — a 'trusted target program' or any unrelated upstream Signer check is NOT sufficient"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-009-invoke-signed","path":"/src/src/lib.rs","start":{"line":430,"col":20,"offset":19286},"end":{"line":430,"col":34,"offset":19300},"extra":{"message":"Jelleo SOL-009: `invoke_signed` CPI. Verify the CALLER has authority for this operation BEFORE the CPI — `invoke_signed` just signs with a PDA, it doesn't validate upstream authority. See: github.com/Copenhagen0x/solana-security-standard#sol-009.","metadata":{"sol-id":"SOL-009","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-009"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the caller's authority for THIS SPECIFIC privileged operation is verified (Signer + has_one/constraint binding the caller to the target resource) BEFORE the invoke_signed — a 'trusted target program' or any unrelated upstream Signer check is NOT sufficient"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-009-invoke-signed","path":"/src/src/lib.rs","start":{"line":500,"col":9,"offset":22018},"end":{"line":500,"col":23,"offset":22032},"extra":{"message":"Jelleo SOL-009: `invoke_signed` CPI. Verify the CALLER has authority for this operation BEFORE the CPI — `invoke_signed` just signs with a PDA, it doesn't validate upstream authority. See: github.com/Copenhagen0x/solana-security-standard#sol-009.","metadata":{"sol-id":"SOL-009","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-009"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the caller's authority for THIS SPECIFIC privileged operation is verified (Signer + has_one/constraint binding the caller to the target resource) BEFORE the invoke_signed — a 'trusted target program' or any unrelated upstream Signer check is NOT sufficient"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":544,"col":24,"offset":23255},"end":{"line":544,"col":30,"offset":23261},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":576,"col":24,"offset":24163},"end":{"line":576,"col":30,"offset":24169},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":588,"col":30,"offset":24528},"end":{"line":588,"col":36,"offset":24534},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":622,"col":24,"offset":25663},"end":{"line":622,"col":30,"offset":25669},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":634,"col":30,"offset":26028},"end":{"line":634,"col":36,"offset":26034},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":655,"col":24,"offset":26774},"end":{"line":655,"col":30,"offset":26780},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":667,"col":30,"offset":27138},"end":{"line":667,"col":36,"offset":27144},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":688,"col":24,"offset":27840},"end":{"line":688,"col":30,"offset":27846},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":700,"col":30,"offset":28204},"end":{"line":700,"col":36,"offset":28210},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":720,"col":24,"offset":28802},"end":{"line":720,"col":30,"offset":28808},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":733,"col":30,"offset":29297},"end":{"line":733,"col":36,"offset":29303},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":755,"col":24,"offset":30081},"end":{"line":755,"col":30,"offset":30087},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":767,"col":30,"offset":30485},"end":{"line":767,"col":36,"offset":30491},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":780,"col":30,"offset":31002},"end":{"line":780,"col":36,"offset":31008},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":801,"col":27,"offset":31688},"end":{"line":802,"col":6,"offset":31699},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}},{"check_id":"rules.solana-security-standard.sol-016-bump-field-read","path":"/src/src/lib.rs","start":{"line":817,"col":27,"offset":32016},"end":{"line":818,"col":6,"offset":32027},"extra":{"message":"Jelleo SOL-016: a stored `.bump` is read. Risk: a non-canonical bump — reused via an Anchor `bump = <account>.bump` constraint, or read and fed to `create_program_address` (both accept non-canonical bumps → PDA substitution). SAFE only if the SAME account's bump was set at init via Anchor's bare `bump`/`ctx.bumps` (canonical), NOT a caller-supplied `#[instruction(bump)]` value — verify that init, then dismiss; otherwise it's the bug. For non-constraint reads compare to `find_program_address(seeds, &program_id).1`. See: github.com/Copenhagen0x/solana-security-standard#sol-016.","metadata":{"sol-id":"SOL-016","category":"security","confidence":"LOW","technology":["solana","anchor"],"references":["https://github.com/Copenhagen0x/solana-security-standard#sol-016"],"license":"MIT","sss-tier":"high","sss-severity":"high","sss-exclusions":["the read is `bump = <account>.bump` (or a bare `seeds = [...], bump` with NO `= target` after bump) on a typed `Account<'info, T>`, AND the SAME account's stored bump was written at init by Anchor's bare `bump` / `ctx.bumps.<name>` (canonical) — NOT a caller-supplied `#[instruction(bump)]` value and NOT a value from create_program_address: verify that exact init site, then dismiss (idiomatic canonical reuse, not a finding). If the init bump is caller-supplied or otherwise non-canonical, this reuse IS the bug — keep it (Anchor's `bump = <expr>` re-derives via create_program_address with the GIVEN bump, so a non-canonical stored bump still validates).","a non-constraint read is validated against the canonical bump before use (e.g. require!(stored == Pubkey::find_program_address(seeds, program_id).1))","the PDA (and its bump) is created in the same instruction using the canonical bump from find_program_address (NOT a caller-supplied bump passed to create_program_address, which accepts any valid off-curve bump and re-introduces the non-canonical-bump bug)"]},"severity":"WARNING","fingerprint":"requires login","lines":"requires login","validation_state":"NO_VALIDATOR","engine_kind":"OSS"}}],"errors":[],"paths":{"scanned":["/src/src/lib.rs"]},"time":{"rules":[],"rules_parse_time":0.004868030548095703,"profiling_times":{"config_time":0.6784822940826416,"core_time":0.7755563259124756,"ignores_time":0.00038909912109375,"total_time":1.4877512454986572},"parsing_time":{"total_time":0.0,"per_file_time":{"mean":0.0,"std_dev":0.0},"very_slow_stats":{"time_ratio":0.0,"count_ratio":0.0},"very_slow_files":[]},"scanning_time":{"total_time":0.09281301498413086,"per_file_time":{"mean":0.09281301498413086,"std_dev":0.0},"very_slow_stats":{"time_ratio":0.0,"count_ratio":0.0},"very_slow_files":[]},"matching_time":{"total_time":0.0,"per_file_and_rule_time":{"mean":0.0,"std_dev":0.0},"very_slow_stats":{"time_ratio":0.0,"count_ratio":0.0},"very_slow_rules_on_files":[]},"tainting_time":{"total_time":0.0,"per_def_and_rule_time":{"mean":0.0,"std_dev":0.0},"very_slow_stats":{"time_ratio":0.0,"count_ratio":0.0},"very_slow_rules_on_defs":[]},"fixpoint_timeouts":[],"prefiltering":{"project_level_time":0.0,"file_level_time":0.0,"rules_with_project_prefilters_ratio":0.0,"rules_with_file_prefilters_ratio":1.0,"rules_selected_ratio":0.1111111111111111,"rules_matched_ratio":0.1111111111111111},"targets":[],"total_bytes":0,"max_memory_bytes":155408832},"engine_requested":"OSS","skipped_rules":[],"profiling_results":[]}
