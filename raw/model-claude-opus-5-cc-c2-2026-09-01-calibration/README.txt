CALIBRATION. Two invocations (wormhole-sysvar, one run per variant) made on 2026-09-01 before the
full sweep, to measure time, context size and API-equivalent cost through the Claude Code CLI.
Not scored. The complete sweep lives in the directory without the -calibration suffix.
